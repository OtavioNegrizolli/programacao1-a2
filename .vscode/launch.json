{
    "configurations": [
        {
            "name": "Attach",
            "port": 9229,
            "request": "attach",
            "envFile": "${workspaceFolder}/.env",
            "skipFiles": [
                "<node_internals>/**"
            ],
            "type": "node"
        }
    ]
}
